% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project_registry_api.R
\name{register_project}
\alias{register_project}
\title{Register A Project For gflowui}
\usage{
register_project(
  project_root,
  project_id = NULL,
  project_name = NULL,
  profile = c("auto", "symptoms_restart", "agp_restart", "custom"),
  graph_sets = NULL,
  condexp_sets = NULL,
  endpoint_runs = NULL,
  defaults = list(),
  scan_results = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{project_root}{Path to the external analysis project root.}

\item{project_id}{Optional project identifier. When \code{NULL}, one is generated
from \code{project_name}.}

\item{project_name}{Display name in the UI.}

\item{profile}{Discovery profile (\code{"auto"}, \code{"symptoms_restart"},
\code{"agp_restart"}, \code{"custom"}).}

\item{graph_sets}{Optional explicit graph-set list to store in manifest.}

\item{condexp_sets}{Optional explicit conditional-expectation set list.}

\item{endpoint_runs}{Optional explicit endpoint run list.}

\item{defaults}{Optional list overriding discovered defaults.}

\item{scan_results}{Whether to run discovery from on-disk outputs.}

\item{overwrite}{Whether to overwrite an existing project with same id.}
}
\value{
Invisibly returns a list with \code{project_id}, \code{manifest_file}, and
the manifest object.
}
\description{
Creates or updates a \code{gflowui} project registry entry with a persisted
project manifest so the project appears in the app \code{Projects} dropdown.
}
\examples{
\dontrun{
register_project(
  project_root = "~/current_projects/AGP",
  profile = "agp_restart",
  project_name = "AGP Restart"
)
}
}
